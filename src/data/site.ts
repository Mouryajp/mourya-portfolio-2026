import { portfolioData } from "@/data/portfolio-data";

export const siteConfig = portfolioData.site.config;
export const navItems = portfolioData.site.navItems;
export const socialLinks = portfolioData.site.socialLinks;
export const contactChannels = portfolioData.pages.contact.channels;
export const chatPrompts = portfolioData.pages.chat.prompts;
